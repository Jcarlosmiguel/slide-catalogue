# certs/

Not tracked in git except this file. Populated by
`catalogue/docs/standalone/generate_selfsigned_cert.sh` - see
`catalogue/docs/standalone/README.md` (Tier 2) for the full setup.

```text
certs/nginx/fullchain.pem       # certificate
certs/nginx/<domain>.key        # private key
certs/nginx/ffdhe2048.pem       # Diffie-Hellman parameters
```
